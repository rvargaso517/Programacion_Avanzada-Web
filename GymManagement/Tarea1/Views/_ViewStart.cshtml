@using Microsoft.AspNetCore.Http
@{
    var rol = Context.Session.GetInt32("Rol");
    if (rol == 1 || rol == 2 || rol == 3)
    {
        Layout = "_LayoutAdmin";
    }
    else
    {
        Layout = "_Layout";
    }
}
